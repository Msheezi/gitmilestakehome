import React from 'react'
import ReactDOM from 'react-dom'
import DragAndDrop from '../draganddrop'
import {render, cleanup, fireEvent} from "@testing-library/react";
import App from '../../App'

// afterEach(cleanup)







"rewards can be added to a lane"
"rewards are placed in the correct swim lane" 
"rewards that are already placed can be moved to a new category "
" rewards can not be placed in the same twice" 
 "rewards can be removed"


"undo/Redo"

"provides an undo and redo button"
"pushing undo removes the last placed reward"